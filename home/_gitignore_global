# Vim
*.un~
.netrwhist
*~

# Ruby
Gemfile.local
Gemfile.local.lock

# Macos
.DS_Store

# Global secrets
.env_secrets
credentials

# Claude files
# .claude/ and CLAUDE.md are deliberately NOT ignored. They are shared project
# config, and Claude Code on the web only sees what is committed — ignoring them
# means a cloud session clones a repo with no conventions, skills, or rules.
# settings.local.json stays private; Claude Code excludes it on its own.
**/.claude/settings.local.json
tasks/
plans/
