# Always color, even if piping to another program
--color

# Always sort the files
--sort-files

# Make sure ack knows how to search common filetypes used in rails projects
--type-add=css=scss

# Rails directories for Ack to ignore
--ignore-dir=.git
--ignore-dir=.bundle
--ignore-dir=.sass-cache
--ignore-dir=log
--ignore-dir=tmp

# Make sure ack knows how to search common filetypes used in nodejs projects
--type-set=json=.json
--type-set=jade=.jade
--ignore-dir=node_modules/
