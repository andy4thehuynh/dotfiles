[github]
	user = andy4thehuynh
[user]
        email = andy4thehuynh@gmail.com
        name = Andy Huynh
[color]
        ui = true
[core]
	editor = nvim -c 'startinsert'
	excludesfile = /Users/andyhuynh/.gitignore
[alias]
        co = checkout
        c = commit -v
        rbc = rebase --continue
        pb = "!git push origin \"$(git rev-parse --abbrev-ref HEAD)\""
        pbf = "!git push -f origin \"$(git rev-parse --abbrev-ref HEAD)\""
        up = "!git pull origin \"$(git rev-parse --abbrev-ref HEAD)\""
        lg = log --graph --pretty=format:'%Cred%h%Creset %C(yellow)%an%d%Creset %s %Cgreen(%cr)%Creset' --date=relative
[init]
	defaultBranch = main
