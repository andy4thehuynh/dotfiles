-- autopairs.lua
--
-- 
-- Add keystrokes to change surrounding brackets, parenthesis, tags, quotes + more

return {
    'windwp/nvim-autopairs',
    event = "InsertEnter",
    opts = {} -- this is equalent to setup({}) function
}
