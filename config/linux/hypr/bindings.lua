-- Personal keybinding overrides. Loaded after Omarchy defaults.
-- Omarchy 4 already ships the app/webapp bindings from the old bindings.conf
-- (SUPER + B browser, SUPER + SPACE launcher, etc.), so this file only holds
-- true overrides.
--
-- See current bindings and descriptions: omarchy menu keybindings --print

-- Example: change SUPER+SPACE from launcher to Omarchy root menu.
-- hl.unbind("SUPER + SPACE")
-- o.bind("SUPER + SPACE", "Omarchy menu", "omarchy-menu toggle root")
