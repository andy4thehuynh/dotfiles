session: exercism

root: ~/Code/exercism

windows:
  - name: editor
    layout: main-horizontal
    commands:
      - vim
    panes:
      - commands:
        - git status
  - name: root
    root: ~/
    commands:
      - tree -L 1
